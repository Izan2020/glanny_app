package com.glantrox.glanny.core.constants

enum class ApiState {none, success, loading, error, empty}

enum class AuthState {login, register, authenticating, success}

enum class FormType {email, password, username}
